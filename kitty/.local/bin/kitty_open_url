#!/bin/bash

url="$1"

if [[ "$url" == file://* ]]; then
  file="${url#file://}"
  elisp="(progn
    (find-file \"${file//\"/\\\"}\")
    (select-frame-set-input-focus (selected-frame)))"

  $(emacsclient --no-wait --eval "$elisp")

  wmctrl -xa emacs
else
  exec xdg-open "$url"
fi
