#!/bin/bash

status=$(wpctl get-volume @DEFAULT_AUDIO_SINK@ | awk '{print ($3=="[MUTED]" ? "off" : "on")}')
vol=$(wpctl get-volume @DEFAULT_AUDIO_SINK@ 2>/dev/null | awk '/MUTED/ {print "muted"} !/MUTED/ {print int($2 * 100) "%"}')
echo "   ${vol}"
